# De trong => frontend goi /api (Vite proxy chuyen tiep sang backend :8001 khi dev).
# Production: dat URL backend day du, vd: VITE_API_BASE_URL=https://api.smartmenu.example
VITE_API_BASE_URL=
# Google OAuth Web client ID. It is public and is embedded in the browser bundle.
VITE_GOOGLE_CLIENT_ID=
