import { AdminRoute } from "../../components/route/AdminRoute";

// Route layout cho khu vuc quan tri: kiem tra quyen admin roi render route con
// qua <Outlet/> ben trong AdminRoute.
export function AdminLayout() {
  return <AdminRoute />;
}
